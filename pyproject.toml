[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "trading-swarm-mcp"
version = "0.1.0"
description = "Local stdio MCP servers extracted from the trading swarm: point-in-time data assembly, pre-trade invariant + leakage auditing, and regime-fragility probing."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "The 1.21 Initiative" }]
keywords = ["mcp", "trading", "backtest-audit", "point-in-time", "regimes"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Financial and Insurance Industry",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Topic :: Office/Business :: Financial :: Investment",
]
dependencies = [
    "mcp>=1.9",
    "httpx>=0.27",
    "numpy>=1.26",
    "pandas>=2.0",
    "scipy>=1.11",
]

[project.optional-dependencies]
test = ["pytest>=8"]
remote = ["uvicorn>=0.30", "starlette>=0.37"]

[project.scripts]
swarm-data-mcp = "swarm_mcp.servers.data_server:main"
swarm-warden-mcp = "swarm_mcp.servers.warden_server:main"
swarm-gym-mcp = "swarm_mcp.servers.gym_server:main"
swarm-mcp-http = "swarm_mcp.servers.http_server:main"

[project.urls]
Homepage = "https://github.com/blink1217/trading-swarm-mcp"

[tool.setuptools.packages.find]
include = ["swarm_mcp*"]

[tool.setuptools.package-data]
"swarm_mcp.vendored.guardrails" = ["*.json", "*.md"]
"swarm_mcp.vendored.alpha" = ["*.md"]

[tool.pytest.ini_options]
testpaths = ["tests"]
